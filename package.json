{
  "name": "malama-labs-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Cryptographic environmental data network",
  "workspaces": [
    "apps/web",
    "contracts/evm",
    "packages/sdk"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace apps/web",
    "start": "npm run start --workspace apps/web",
    "test:all": "npm run test --workspaces --if-present",
    "lint:all": "npm run lint --workspaces --if-present",
    "build:all": "npm run build --workspaces --if-present",
    "postinstall": "node scripts/patch-libsodium-wrappers.js"
  },
  "author": "Mālama Labs",
  "license": "UNLICENSED",
  "dependencies": {
    "vercel": "^51.2.1"
  }
}
